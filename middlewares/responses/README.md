# Responses Middleware

## Usage


Use the following methods.

* `ctx.badRequest(data)`
* `ctx.created(data)`
* `ctx.forbidden(data)`
* `ctx.notFound(data)`
* `ctx.ok(data)`
* `ctx.serverError(data)`


